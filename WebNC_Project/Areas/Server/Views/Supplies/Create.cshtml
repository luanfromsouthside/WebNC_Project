@model WebNC_Project.Models.Supply

@{
    ViewBag.Title = "Thêm mới vật tư";
}

<div class="card">
    <div class="header">
        <h2>
            @ViewBag.Title.ToUpper()
        </h2>
    </div>
    <div class="body">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)
            @Html.LabelFor(s => s.ID)
            <div class="form-group">
                <div class="form-line">
                    @Html.EditorFor(s => s.ID, new { htmlAttributes = new { @class = "form-control" } })
                </div>
                @Html.ValidationMessageFor(s => s.ID, "", new { @class = "text-danger" })
            </div>
            @Html.LabelFor(s => s.Name)
            <div class="form-group">
                <div class="form-line">
                    @Html.EditorFor(s => s.Name, new { htmlAttributes = new { @class = "form-control" } })
                </div>
                @Html.ValidationMessageFor(s => s.Name, "", new { @class = "text-danger" })
            </div>
            @Html.LabelFor(s => s.Total)
            <div class="form-group">
                <div class="form-line">
                    @Html.EditorFor(s => s.Total, new { htmlAttributes = new { @class = "form-control" } })
                </div>
                @Html.ValidationMessageFor(s => s.Total, "", new { @class = "text-danger" })
            </div>
            <button class="btn btn-primary waves-effect" type="submit">SUBMIT</button>
        }
    </div>
</div>
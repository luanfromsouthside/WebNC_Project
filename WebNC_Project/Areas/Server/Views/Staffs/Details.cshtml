@model WebNC_Project.Models.Staff

@{
    ViewBag.Title = "Thông tin nhân viên";
}
@section styles {
    <link href="~/Areas/Server/resources/plugins/sweetalert/sweetalert.css" rel="stylesheet" />
}
<div class="card profile-card">
    <div class="profile-header">&nbsp;</div>
    <div class="profile-body">
        <div class="content-area">
            <h3>Nhân viên : @Model.ID</h3>
            <p class="font-18">Họ tên: @Model.Name</p>
            <p class="font-18">Ngày sinh: @Model.Birth.ToString("dd/MM/yyyy")</p>
            <p class="font-18">Giới tính: @(Model.Gender ? "Nam" : "Nữ")</p>
            <p class="font-18">SĐT: @Model.Phone</p>
            <p class="font-18">Email: @Model.Email</p>
            <p class="font-18">Chức vụ: @Model.Permission.Name</p>
        </div>
    </div>
    <div class="profile-footer">
        <div class="btn-group btn-group-justified" role="group" aria-label="Justified button group">
            <a href="@Url.Action("Edit","Staffs", new { id = Model.ID.Trim() })" class="btn btn-primary waves-effect">
                <i class="material-icons">edit</i>
                <span>EDIT</span>
            </a>
            <a href="javascript:void(0);" role="button" class="btn btn-danger waves-effect" id="btnRemove">
                <i class="material-icons">remove_circle</i>
                REMOVE
            </a>
        </div>
    </div>
</div>
@section scripts{
    <script src="~/Areas/Server/resources/plugins/sweetalert/sweetalert.min.js"></script>
    <script>
        $(function () {
            $('#btnRemove').on('click', function () {
                remove();
            });
        });
        function remove() {
            swal({
                title: "Do you want to remove this staff?",
                text: "Staff ID: @Model.ID",
                type: "warning",
                showCancelButton: true,
                closeOnConfirm: false,
                showLoaderOnConfirm: true,
            }, function () {
                    $.ajax({
                        type: 'POST',
                        url: '@Url.Action("Remove","Staffs")',
                        data: { id: '@Model.ID.Trim()' },
                        success: function () {
                            swal("Remove success!");
                            setTimeout(function () {
                                location.href = "@Url.Action("Index")";
                            }, 500);
                        },
                        complete: function (xhr) {
                            swal(xhr.status);
                        },
                        error: function () {
                            swal("Error when remove, Please try again");
                            setTimeout(function () {
                                location.reload();
                         }, 500);
                     }
                });
            });
        }
    </script>
}